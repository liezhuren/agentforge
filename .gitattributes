# 统一换行符：仓库里一律 LF。
#
# 为什么需要这条（一次真实事故）：
# 用 PowerShell 的 `Set-Content` 重写一个源文件时，它按平台默认写成了 **CRLF**，
# 于是**一个 60 行的改动变成了 3608 行的 diff** —— 而语义完全没变。
# 改动不可审阅比改动本身更麻烦：没人能看出真正改了哪几行。
#
# 这与 `.gitignore` 里那条注释是同一个道理：**规则比记性可靠。**
# 与其每次提醒自己「别用 PowerShell 文本 cmdlet 改源码」，
# 不如让 git 在提交时就把它规范化 —— 这样即使工具用错了，仓库里的内容也还是对的。
#
# 注意：这条只统一换行符，不改变文件编码。编码问题（PS 5.1 的 Get-Content 按 ANSI/GBK 读
# UTF-8 文件会把中文读成乱码）git 管不了，只能靠「改 UTF-8 源码一律用 edit/write 工具」。
* text=auto eol=lf

# 二进制文件不做任何转换
*.png binary
*.ico binary
*.woff2 binary
